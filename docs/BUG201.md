# BUG #201 — x86→ARM64 GC-runtime heap-corruption miscompile

Status: **OPEN** (x86_64 guests only; does not affect arm64 guests or the v0.9.6 showcase).
This is the consolidated record of a multi-agent investigation. The live working set (tooling patch,
repro binaries, evidence log) lives under `target/heap201/` (untracked — `target/` is gitignored); this
doc is the tracked summary so the knowledge survives.

## Symptom
A GC'd Go runtime silently corrupts live heap data under dd (x86_64 guest). Canonical repro: a
single-goroutine `map[string]int` + GC/alloc churn (`target/heap201/mcount`, GOMAXPROCS=1). ~40% of runs,
a contiguous insertion-order range of live strings reads back as zeros. On the amd64 oracle and under
`GOGC=off` it ALWAYS passes. Cascades in the wild to **go build, gcc, .NET, clang -O2, CPython-x86,
rustc** — any GC'd/managed runtime. Plain C/C++ (incl. vtables) is unaffected.

## Converged conclusion (mechanism — high confidence)
Go uses the **tiny allocator** for the 10–13B noscan key strings. When it fetches a fresh 16-byte block,
`mallocgc` zeroes it with one 16-byte SIMD store — x86 `movups %xmm15,(%r11)` at mallocgc link ~`0x40cff3`,
lowered by dd to `str q15,[x17]` (ARM `0x3d80022f`). On a corrupting run, the block address (`r11` → dd's
EA `x17`) is a **LIVE tiny block still holding a marked, in-use string**, so this memclr **zeros a live
string in place**. The string header (ptr+len) stays intact; only its bytes go to zero → subsequent map
lookups / `keys[i]` compares fail. Deterministic under `DDFIXHEAP` starting at `firsti=1014` (~410 objects).

Unperturbed ground truth (repro `msweep7`): `curPtr==origPtr`, `curLen==13`, bytes = `\x00`×13; the victim
span is **MARKED and NEVER freed** (sweep inspection every span: setbits=410, allocCount=512). So it is
**not** free-then-reuse — it is a direct wrong-destination memclr while the object is still live.

**Where the wrong destination comes from (still to pin):** the store EA is just `[x17]`, `x17 = r11` = the
block address from the inlined **nextFreeFast** (or the tiny bump `c.tiny+tinyoffset`). So the allocator
handed out a block that is actually a live/marked slot. The value/layout-dependent codegen bug is in the
guest instructions computing that block address — the inlined nextFreeFast / allocCache / freeindex path,
or the tiny bump. It's ASLR/seed-dependent (~40%), which is why every static audit passed (each
computation verifies correct on non-corrupting runs).

## Verified CORRECT — do NOT re-audit (all 0-mismatch, several confirmed on corrupt runs)
- `scanblock` root/stack pointer load (0x41d520; >1.5M loads).
- `typePointers.next` slot returns; `findObject` arenaIndex `(p+0x800000000000)>>26`, pageIndex
  `(p>>13)&0x1fff`, objIndex divMagic `(off*divMul)>>32`.
- `greyobject` mark-store (`lock or %r12b,(%r10)`, bytep = gcmarkBits+objIndex/8) — bit is set.
- `sweep` allocBits/gcmarkBits swap, newMarkBits sizing, countAlloc.
- `refillAllocCache` structure (`allocCache = ^allocBits[word]`); both nextFreeFast inlinings return
  UNMARKED slots on non-corrupt runs.
- Lowerings: `not r64`→`orn`, `bsf/bsr`, `btc`, base `emit_ea_core`, the block-zeroing `movups`→`str q15`
  EA itself (`x17==r11` exactly, no truncation for high heap addrs).
- (Angle-B deep differential, native x86 oracle vs dd, `bad=0`): BSF/BSR/TZCNT/LZCNT/POPCNT **value AND
  ZF/CF flags**; the allocCache `SHRQ %cl` + shift-by-≥64 guard; and the `result=freeindex+bit;
  addr=base+index*elemsize` arithmetic (IMUL/ADD/`SHL $4`/LEA) over all size classes + high heap bases
  (incl. the 5 TB DDFIXHEAP region). The next-free computation is correct IN ISOLATION → the fault is a
  CONTEXT/ordering interaction on an individually-correct instruction, NOT a value-wrong opcode.
- (Angle-A per-alloc trace, DDFIXHEAP deterministic, ~80k tiny allocs/corrupt-run, every probe valid vs
  the PERSISTENT allocBits at span+0x40): BOTH the tiny-**bump** (`c.tiny + round(tinyoffset,align)` @
  guest 0x40ced6) AND nextFreeFast/nextFree (@0x40cfa6) return a genuinely FREE slot on EVERY allocation
  on corrupting runs — `allocBitSet=0`, `markBitSet=0`, `overflowLive=0`, `LIVE-HANDOUT=0`. The ALLOCATOR
  IS EXONERATED; there is NO first-diverging allocation. The block-zero `movups %xmm15,(%r11)` @0x40cff3
  (→ dd `str q15,[x17]`) is NOT the culprit — its dest r11 is verified-free every time.

## Hypothesis #1 (wrong block address) is REFUTED — the corrupting store is elsewhere
Both Angle A (allocator trace) and Angle B (bit-scan/arith differential) agree the tiny-allocator handout
is correct. So the corrupting 16-byte zero-store has a destination computed OUTSIDE the allocator: a
DIFFERENT `str q`-of-zero / memclr whose **EA is miscomputed on the corrupt layout**, landing on a live
tiny slot. Prime suspect: **`memclrNoHeapPointers` @guest 0x463440** (UNRESOLVED — range-sampling probes
perturb it to 0%; needs a start-slot-only allocBits check or emitter-level instrumentation). Per Angle B,
the wrong EA comes from an individually-correct instruction → a scratch-reg (x16/x17) clobber across an
EA-fold / translation-block boundary. **Guest PC map (mcount, runtime=link+0x200000000):** mallocgc
0x40cd00; tiny-bump ret `lea (%r12,%r10,1),%rax` 0x40ced6; inlined nextFreeFast 0x40cf1d–0x40cfa6;
block-zero 0x40cff3; memclrNoHeapPointers 0x463440.

## RECOMMENDED NEXT PROBE (Angle A handoff) — emitter-level allocBits trap
Instrument dd's EMITTER so every `str q`-of-zero / memclr-class store emits an inline guard that TRAPS when
its destination EA has allocBits set AND EA ∈ the Go arena (0xc0.. region). The trapping store's guest PC +
its `x17` (actual EA) vs the intended destination is the answer — it names the miscompiled instruction and
whether x17 was clobbered. This is emitter-side, not a pre-instruction brk (brk can't reach memclr's
mid-body stores). Angle A's diagnostic hunks (env-gated, uncommitted) live in worktree
`agent-aad774950ecdddc2d`: mem.c DDFIXHEAP, translate/x86_64/elf.c alloc_probe + dual-brk, engine_glue.c
DDUMP3 — reusable scaffolding.

## SUPERSEDING CONCLUSION (both store-angles done): GC MARK-MISS / SWEEP, not a store
The emitter-level allocBits trap (run to completion) OVERTURNED the wrong-EA memclr pivot above:
- The victim is ONE deterministic 8 KB tiny span **0xc000102000–0xc000104000** (512×16B slots;
  `unsafe.StringData(keys[1014])=0xc000102000`), fully zeroed.
- The store that zeros it IS the ordinary CORRECT tiny block-zero (`mallocgc+0x2f3` @0x40cff3, str q15),
  and **allocBits==0 at zero-time on every corrupt run**. So the allocator is CORRECTLY re-handing-out +
  re-zeroing that span because allocBits legitimately says it's free. Neither the block-zero nor
  memclrNoHeapPointers has a bad EA. The `## Hypothesis` and `## RECOMMENDED NEXT PROBE` blocks above are
  SUPERSEDED — the corrupting store is innocent.
- ROOT: a whole LIVE 8 KB span (512 noscan 13-byte string backings for keys[1014..], reachable via the
  `keys []string` backing array) loses its allocBits → freed → correctly reused. This is a **mark-miss or
  a sweep-clears-live-span bug** — HANDOFF.md's original converged suspect (scanobject leaves the span
  unmarked → swept), NOT a codegen store bug.

Angle-C translation-bisect (N≥40, DDFIXHEAP): NO optimization toggle flips it clean — NOTIER2/NOTIER2X,
NOFLAGELIDE, NOSSEOPT, NOEAOPT, NOGUESTFOLD, NOREP, NOREPCMP all leave it intact; NOLSE is a no-op for x86
guests. → the responsible construct is in an ALWAYS-ON BASELINE lowering (baseline scanobject bitmap-word
load + bit iteration + slot greying, OR the sweep allocBits/gcmarkBits swap), not an optimized fast-path.

METHODOLOGY CORRECTIONS (invalidate earlier controls):
- `env GOGC=off <engine>` NEVER reaches the guest: the x86 engine builds guest env from DD_GUEST_ENV +
  a fixed {PATH,HOME,TERM,LANG} only (translate/x86_64/elf.c:355). Host GOGC/GODEBUG are DROPPED. Use
  `DD_GUEST_ENV=$'GODEBUG=gctrace=1\n'`. Any prior bare-env "GOGC=off" control was INVALID.
- Even correctly forwarded, GOGC=off does NOT fix mcount (it calls runtime.GC() every 100 passes = forced
  GCs). The real invariant is **"no explicit runtime.GC() ⇒ no corruption"** (mnogc = mcount minus the
  runtime.GC() line = 0/30). Trigger = a large tiny-alloc population accumulated between sparse FORCED GCs.
- DDFIXHEAP determinism is per-binary + layout-fragile: recompiling the repro can move/kill the victim →
  `target/heap201/mcount` remains the canonical repro; do NOT rely on a reimplementation.

## DECISIVE NEXT PROBE: watch span 0xc000102000 gcmarkBits across the pre-corruption GC
Get the span struct for 0xc000102000 (arena walk / alloc_probe), then watch gcmarkBits (span+0x48) and
allocBits (span+0x40) across the forced GC that precedes corruption: (a) if the 512 slots are NEVER
mark-set during mark → MARK-MISS → audit the BASELINE x86 lowering of scanobject / typePointers.next
reaching the `[]string` backing array's Data pointers into greyobject (the greying store is verified; the
miss is in the scan REACHING them). (b) if marked yet swept → the sweep allocBits/gcmarkBits swap. Then
add an env-gated alternate lowering of ONLY that baseline sequence and diff corrupt-rate on mcount.
- RULED OUT: async-preempt (`asyncpreemptoff=1`), non-PIE bias fold (`NOGUESTFOLD`), EA-opt (`NOEAOPT`),
  rep-stos/movs (`NOREP`), AES-NI, lazy zero-page guard (`NOLAZYFIX=1 LAZYBUDGET=0`), TSO, FS-base.
- `GOGC=off` fixes it 100% → it is a GC-window bug; a standalone noscan `[]byte` victim is IMMUNE (trigger
  needs scanning/allocating pointer-adjacent objects through the tiny allocator).

## The obstacle (why it's still not pinned)
**Every observation method perturbs the bug away:**
- DDUMP `brk` breakpoints in the GC/alloc hot path slow GC enough that corruption → ~0%.
- DDWATCH RO page-protection on the victim either (a) is far too slow with emulate-and-continue (millions
  of legit tiny-alloc faults → timeout) or (b) shifts the victim off the protected pages (protecting the
  victim moves the manifestation to a different, unprotected object). 0 catches either way.

So the corrupt case is only ever seen WITHOUT instrumentation — which gives the effect but not the
faulting instruction. A **non-breakpoint, non-page-protection** method is required.

## Recommended next approach
**Differential per-alloc trace (lowest-overhead, avoids perturbation):** emit an inline ring-buffer log of
the tiny block addresses `nextFreeFast` returns (from a cheap emitted stub, NOT a signal/breakpoint),
run the SAME deterministic binary (`DDFIXHEAP`) corrupt-vs-clean-vs-amd64-oracle, and find the **first
divergent block address** — that is where the allocator computes wrong.

Suspect instructions to scrutinize in dd's lowering for a value/high-bit-dependent miscompile (the string
span's allocCache word + freeindex/index math):
- the `shr %cl` allocCache update with the `cmp $0x40; sbb; and` shift-by-64 guard (mallocgc
  `0x40cf77-85` / `0x40d139-146`),
- the `bsf` on allocCache,
- the `imul result,elemsize; add base` slot-address (`0x40d163-167`).

## Tooling (in `target/heap201_tooling.patch`, x86 target only, env-gated, INERT by default)
- **`DDFIXHEAP=1`** — deterministic guest heap+image layout: non-PIE bias `0x200000000` (runtime addr =
  link addr + 0x200000000), fixed mmap base 5 TB, image base 8 GB, fixed AT_RANDOM. Makes the corruption
  deterministic (firsti=1014).
- **`DDUMP=<hexRuntimePC>`** — `brk #0x5202` at that guest PC; the SIGTRAP handler in
  `translate/x86_64/elf.c` (`jit86_syncguard`) dumps live guest regs from the ARM ucontext.
- **`DDWATCH=1`** + guest `madvise(addr,len,0x5201)` — mprotects RO; a wrong store faults and
  `jit86_lazyguard` dumps ucontext regs + the ARM store insn + `x17`=EA. Includes `watch_slot_marked`
  (arena walk + mark-bit read) and `watch_emulate_store` (emulate-and-continue, reusing `nonpie_fixup`'s
  ARM decode-and-perform machinery).

mspan offsets (Go 1.22): base+0x18, freeindex+0x30(u16), nelems+0x32(u16), allocCache+0x38, allocBits+0x40,
gcmarkBits+0x48, allocCount+0x60(u16), spanclass+0x67, elemsize+0x68. Arenas L1 ptr for the span walk:
mcount link `0x54c7b8` → biased `0x20054c7b8` (env `DDARENAS`).

## Artifacts (all under `target/heap201/`, untracked)
- **`BUG201_EVIDENCE.md`** — primary consolidated evidence (this doc is its tracked digest); has the full
  "CONVERGED CONCLUSION (READ THIS FIRST)" section, addresses/offsets, and repro list.
- **`HANDOFF.md`** — agent handoff: tooling, build/run/reap discipline (incl. the stale-engine trap), and
  the running "LIVE FINDINGS" log.
- **`../heap201_tooling.patch`** — the diagnostic-tooling diff (apply to a worktree; regenerate from the
  tree for the latest snapshot).
- **Repro binaries + sources** (linux/amd64, engine-independent): `mcount` (canonical), `msweep7`
  (header-vs-bytes ground truth), `mtight2`, `mgrey2`, `mnoscan`, `mglobal`, `mnomap`, `mboth`, `mw6/mw7`
  (DDWATCH catch, too slow), + `src_*.go`.
- **`verify.sh <engine> <repro> [N]`** — corrupt-rate harness.
- The multi-agent final reports are in the session task transcripts under
  `/tmp/claude-501/-Users-x-dd/24e2b9da-35b5-49af-862e-f31faca9ad62/tasks/`; agent C's report (object stays
  marked, bytes zeroed in place, tiny-allocator memclr) is the decisive one.

## Notes
- Fix cascades: pinning + fixing this one allocator-address miscompile is expected to unblock go build,
  .NET, clang -O2, CPython-x86, and likely rustc at once.
- A background non-perturbing catch (`mw7` small-region) was still running at the time of writing; fold its
  result into `target/heap201/BUG201_EVIDENCE.md` when it lands.
